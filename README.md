# Travel Together

Travel Together is a group travel planning app with a React frontend and an Express + TypeScript backend.

## Tech Stack

- Frontend: React + TypeScript
- Backend: Node.js + Express + TypeScript
- Data/Auth: Supabase

## Quick Start

1. Install dependencies:
   - `cd backend && npm install`
   - `cd ../frontend && npm install`
2. Start backend:
   - `cd backend && npm run dev`
3. Start frontend:
   - `cd frontend && npm start`

## Environment Variables

Frontend (`frontend/.env`):

- `EXPO_PUBLIC_SUPABASE_URL`
- `EXPO_PUBLIC_SUPABASE_ANON_KEY`
- `REACT_NATIVE_API_URL` (optional, defaults to `http://localhost:3001`)

Backend:

- `PORT` (optional, defaults to `3001`)
- `RAPIDAPI_KEY`

# AI Credit

https://github.com/slee622/HCI_Project_Trip_Together/blob/main/DESIGN_DECISIONS.md is a list of all major parts of the codebase that AI was used for.
